{
  "schema": "surface-config/v1",
  "environment": "imprint",
  "repo_path": "/Users/anthonyflores/code/imprint",
  "canonical": true,
  "role": "Personal-infra memory + taste system: ingest quarantine, entity registry, operator data root. Rich-lineage fork.",
  "default_branch": "main",
  "gh_repo": "anthony-ai-systems/imprint",
  "cold_load": [
    {
      "claim_template": "HEAD is {value}",
      "probe": {"kind": "git-head"}
    },
    {
      "claim_template": "HEAD committed {value}",
      "probe": {"kind": "git-head-time"}
    },
    {
      "claim_template": "open PRs: {value}",
      "probe": {"kind": "gh-pr-count"}
    },
    {
      "claim_template": "package present (src/imprint/): {value}",
      "probe": {"kind": "file-exists", "path": "src/imprint"}
    },
    {
      "claim_template": "project metadata present (pyproject.toml): {value}",
      "probe": {"kind": "file-exists", "path": "pyproject.toml"}
    }
  ],
  "verification": [
    {
      "name": "imprint health",
      "notes": "imprint runtime (store/WAL/hooks) health -- exit code only, content-free; repo SHA says nothing about whether the memory system is capturing. Guards the silent-WAL-lockout failure mode (bit twice Jul 2026).",
      "probe": {
        "kind": "script-exit-code",
        "script": "/Users/anthonyflores/.local/lib/imprint-local/venv/bin/imprint",
        "args": ["health"],
        "timeout": 30
      }
    }
  ],
  "decisions": [],
  "next_lanes": [],
  "do_not_read": [
    {"entry": "dist/ + release-artifacts/ -- build/release outputs; regenerate, do not read as source.", "source": "config", "as_of": "2026-07-23"}
  ],
  "pitfalls": [
    {"entry": "Rich-lineage fork: intelligence-layer changes live HERE (diffable vs RichSchefren upstream), never in the wiki-vault.", "source": "operator-memory", "as_of": "2026-07-23"},
    {"entry": "Fork history is disjoint from upstream (blocks straight upstreaming); reconcile deliberately, do not assume a shared base.", "source": "operator-memory", "as_of": "2026-07-23"},
    {"entry": "Never merge or direct-push to a default branch -- merge is Anthony's.", "source": "operator-memory", "as_of": "2026-07-23"}
  ]
}
